let a = prompt("Вкажіть Bаш вік");

let age = "i"
let count = age
let result = ""
if (count >=10 && count <=20) {
    result = "років"
} else {
    count = age % 10

    if(count === 1) {
        result = "рік"}
    else if (count >=2 && count<=4){
        result = "роки"
    }
    else if (count >=2 && count<=4){
        result = "роки"
        }
    else if (age < 0) {
        console.log(Error)
    }
    else if (age = isNaN) {
        console.log(Error)
    }
    else {result = "років"}
    console.log (age + " " + result);}









